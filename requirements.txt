# ─────────────────────────────────────────────────────────────
# AarogyaKosha — Python Dependencies (All Free & Open Source)
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9        # file uploads

# ── Database ──────────────────────────────────────────────────
# SQLite is built into Python (dev)
# For production PostgreSQL:
# psycopg2-binary==2.9.9
# sqlalchemy==2.0.30
# alembic==1.13.1

# ── OCR (Document Extraction) ─────────────────────────────────
# Tesseract wrapper — free, open source
pytesseract==0.3.10
Pillow==10.3.0                 # Image processing
pdf2image==1.17.0              # PDF → image conversion
# System deps: sudo apt-get install tesseract-ocr tesseract-ocr-hin tesseract-ocr-tel poppler-utils

# ── Clinical NLP ──────────────────────────────────────────────
# Hugging Face Transformers — free, open source models
transformers==4.41.0
torch==2.3.0                   # PyTorch (CPU build OK for inference)
# Model: emilyalsentzer/Bio_ClinicalBERT (free, no API key needed)
# Model: d4data/biomedical-ner-all (free NER)
spacy==3.7.4                   # NLP pipeline
# python -m spacy download en_core_web_sm

# ── LLM for Translation (Local — No API Key) ──────────────────
ollama==0.2.0                  # Run Llama3/Mistral locally, no API key
# OR: use the Anthropic API (paid) for higher quality
# anthropic==0.28.0

# ── FHIR ──────────────────────────────────────────────────────
fhir.resources==7.1.0          # FHIR R4/R5 Python models (free)

# ── Vector Search (for RAG) ───────────────────────────────────
sentence-transformers==3.0.1   # Free embedding models
# pgvector: install Postgres extension + pip install pgvector

# ── LangChain (Agentic workflows) ────────────────────────────
langchain==0.2.5
langchain-community==0.2.5

# ── File Processing ───────────────────────────────────────────
PyMuPDF==1.24.5                # Fast PDF parsing (fitz)
python-magic==0.4.27           # MIME type detection

# ── Security & Auth ───────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # Password hashing
cryptography==42.0.5               # AES-256 encryption

# ── Background Tasks ──────────────────────────────────────────
celery==5.4.0                  # Async task queue
redis==5.0.4                   # Celery broker (optional, sqlite fallback)

# ── HTTP Client ───────────────────────────────────────────────
httpx==0.27.0                  # Async HTTP (for ABDM API calls)

# ── Data & Utils ──────────────────────────────────────────────
pydantic==2.7.0
python-dotenv==1.0.1
pandas==2.2.2                  # Lab trend analysis

# ── QR Code Generation ────────────────────────────────────────
qrcode[pil]==7.4.2             # Emergency QR codes

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0                  # Test client
